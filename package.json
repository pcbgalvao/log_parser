{
  "name": "log_parser",
  "version": "0.5.0",
  "description": "Commnad Line Log Parser",
  "main": "app/log_parser.js",  
  "scripts": {    
    "build": "pkg -c package.json -o build/log_parser -t node17-linux app/log_parser.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },  
  "author": "Pedro Galvão",
  "license": "ISC"
}
